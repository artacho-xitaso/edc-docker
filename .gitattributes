# Ensure shell scripts always use LF line endings (never CRLF)
# so they work correctly when mounted into Linux containers.
*.sh text eol=lf

# Config files used inside containers — keep LF
*.hcl  text eol=lf
*.properties text eol=lf
Caddyfile text eol=lf
